``` bash
cd ~/Workspace/hello-docsify
git init
touch README.md
git add .
git commit -m "first commit"
git remote add origin http://gogs.qinlei.xyz/qinlei/hello-docsify.git
git push -u origin master

# 加了参数 -u 后，以后即可直接用 git push 代替 git push origin master
```